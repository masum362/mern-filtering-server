const login = (req ,res) => {
    console.log('loggin called');
};

const register = (req ,res) => { 
    const user = req.body;

    // const isAlreadyRegistered = await


};


export {login ,register};